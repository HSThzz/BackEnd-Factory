import { Router } from 'express';

const router = Router();

// Root route
router.get('/', (req, res) => {
  res.json({
    message: 'Welcome to {{projectName}} API',
    version: '{{version}}',
  });
});

export default router;
